```
const createBoard = require('../../utils/createBoard').default;
const board = createBoard();

<Board board={board} />
```

Small:

```
const createBoard = require('../../utils/createBoard').default;
const board = createBoard();

<Board board={board} size='small'/>
```
